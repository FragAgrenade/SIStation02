@model IEnumerable<sistation.Models.Quiz>

    @{
    ViewData["Title"] = "Lista de Quizzes";
    }

    <h1>Quizzes</h1>

    <p>
        <a asp-action="Create" class="btn btn-primary">Adicionar Novo Quiz</a>
    </p>

    <table class="table">
        <thead>
            <tr>
                <th>Descrição</th>
                <th>Usuário</th>
                <th>Ações</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model) {
            <tr>
                <td>@item.Description</td>
                <td>@item.User.Email</td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-primary">Alterar</a> |
                    <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-primary">Detalhes</a> |
                    <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-primary">Apagar</a>
                </td>
            </tr>
}
        </tbody>
    </table>
